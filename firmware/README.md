# Firmware

This folder will hold the ESP32 firmware (`maceration.ino` + `config.h`) once
Fase 5 (Firmware ESP32) in TASKS.md is reached.

See `docs/rancangan-website-iot-maceration.md` section 7 for the planned
additions (WiFi, MQTT, telemetry publish, command handling).
